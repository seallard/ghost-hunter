# Clerk — get keys from https://dashboard.clerk.com (Development instance)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Where Clerk's hosted components send users
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# Postgres — local default matches docker-compose.yml; Railway injects via the Postgres plugin
DATABASE_URL=postgres://ghost:ghost@localhost:5432/ghost_hunter

# Object storage — Railway bucket for cover-letter PDFs. Names match the
# variables Railway's Bucket plugin exposes; reference them directly on the
# Next.js service (e.g. ENDPOINT=${{Bucket.ENDPOINT}}). REGION is optional
# and defaults to us-east-1. Local dev does not require these set; uploads
# will 500 without them.
ENDPOINT=
ACCESS_KEY_ID=
SECRET_ACCESS_KEY=
BUCKET=
REGION=us-east-1
